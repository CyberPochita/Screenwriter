import { useState } from 'react';
import 'panel.css';

function initPanel(type: string) {
    const = 
}

function Panel() {
    const [list, setList] = useState();

    return(
        <>

        </>
    )
}